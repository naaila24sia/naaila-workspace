import { createRoot } from "react-dom/client";
import "./tailwind.css";
import DaftarEvent from "./DaftarEvent";

createRoot(document.getElementById("root"))
    .render(
                <DaftarEvent/>
    )